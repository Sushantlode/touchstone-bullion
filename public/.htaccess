Options -MultiViews
RewriteEngine On
RewriteBase /

# SPA fallback — send deep routes ( /about, /contact, … ) to index.html
RewriteRule ^index\.html$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
